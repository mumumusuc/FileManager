<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group
        android:checkableBehavior="single"
        android:orderInCategory="1">
        <item
            android:id="@+id/action_style_list"
            android:checkable="true"
            android:checked="true"
            android:icon="@drawable/ic_tool_style_list"
            android:title="@string/tool_title_list"
            app:showAsAction="always" />
        <item
            android:id="@+id/action_style_grid"
            android:checkable="true"
            android:checked="false"
            android:icon="@drawable/ic_tool_style_grid"
            android:title="@string/tool_title_grid"
            app:showAsAction="always" />
    </group>
    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_tool_search"
        android:orderInCategory="99"
        android:title="@string/tool_title_search"
        app:showAsAction="always" />
</menu>
